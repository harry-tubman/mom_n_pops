

<% if current_user.designation == "Community Organization" %>

    <%= render 'org_form' %>

<% else %>

    <%= render 'vendor_form' %>

<% end %> 

<%= link_to 'Back', listings_path, class: "btn btn-link" %>
